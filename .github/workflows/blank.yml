name: CI
on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo ${{ secrets.KUBECONFIG_BASE64 }}
      - run: sh .ci/setup-kubeconfig.sh
        env:
          KUBECONFIG_BASE64: ${{ secrets.KUBECONFIG_BASE64 }}
      - run: env
        env:
          KUBECONFIG_BASE64: ${{ secrets.KUBECONFIG_BASE64 }}
      - run: ls -al
      - uses: docker://sikalabs/dev
        with:
          args: tree
      - uses: docker://sikalabs/ci
        with:
          args: sh .ci/deploy.sh
